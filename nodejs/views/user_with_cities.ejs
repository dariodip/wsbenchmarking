<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>All User Query</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
<div>
    <h1>All Users with City Info</h1>
    <table style="width: 100%">
       <tr>
            <th>id</th>
            <th>name</th>
            <th>surname</th>
            <th>city id</th>
            <th>city name</th>
            <th>post code</th>
            <th>region</th>
        </tr>
        <% for(let i = 0; i < uwc.length; i++) {%>
            <tr>
                <td><%= uwc[i].id %></td>
                <td><%= uwc[i].name.capitalize() %></td>
                <td><%= uwc[i].surname.capitalize() %></td>
                <td><%= uwc[i].city_id %></td>
                <td><%= uwc[i].city.name.capitalize() %></td>
                <td><%= uwc[i].city.post_code %></td>
                <td><%= uwc[i].city.region.capitalize() %></td>
            </tr>
        <%}  %>
    </table>
</div>

</body>
</html>