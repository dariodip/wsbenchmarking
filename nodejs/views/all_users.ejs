<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>All User Query</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
<div>
    <h1>All Users Info</h1>
    <table style="width: 100%">
        <tr>
            <th>id</th>
            <th>name</th>
            <th>surname</th>
            <th>city id</th>
        </tr>
        <% for (let i = 0; i < users.length; i++) {%>
            <tr>
                <td><%= users[i].id %></td>
                <td><%= users[i].name.capitalize() %></td>
                <td><%= users[i].surname.capitalize() %></td>
                <td><%= users[i].city_id %></td>
            </tr>
        <%} %>

    </table>
</div>

</body>
</html>